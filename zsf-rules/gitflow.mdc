---
description: Gitflow 工作流规则。执行 git 操作时应遵循这些规则。
---
# Gitflow 工作流规则

## 主分支

### main（或 master）
- 包含可发布的生产代码  
- 禁止直接提交到 main  
- 仅接受以下分支的合并：  
  - hotfix/* 分支  
  - release/* 分支  
- 每次合并后必须用版本号打标签  

### develop
- 主开发分支  
- 包含最新已交付的开发变更  
- 作为 feature 分支的源分支  
- 禁止直接提交到 develop  

## 支持分支

### feature/*
- 从 develop 分支创建  
- 合并回 develop  
- 命名规范：feature/[issue-id]-描述性名称  
- 示例：feature/123-user-authentication  
- 在创建 PR 前必须同步最新 develop  
- 合并后删除分支  

### release/*
- 从 develop 分支创建  
- 合并回：  
  - main  
  - develop  
- 命名规范：release/vX.Y.Z  
- 示例：release/v1.2.0  
- 仅允许 bug 修复、文档更新和发布相关任务  
- 禁止引入新功能  
- 合并后删除分支  

### hotfix/*
- 从 main 分支创建  
- 合并回：  
  - main  
  - develop  
- 命名规范：hotfix/vX.Y.Z  
- 示例：hotfix/v1.2.1  
- 仅用于紧急生产环境修复  
- 合并后删除分支  

## 提交消息

- 格式：`type(scope): description`  
- 类型：  
  - feat：新功能  
  - fix：Bug 修复  
  - docs：文档变更  
  - style：格式、缺失分号等  
  - refactor：代码重构  
  - test：添加测试  
  - chore：维护任务  

## 版本控制

### 语义化版本
- MAJOR：不兼容的 API 修改  
- MINOR：向下兼容的功能新增  
- PATCH：向下兼容的 Bug 修复  

## 合并请求规则

1. 所有变更必须通过 Pull Request  
2. 必需审批：至少 1 人  
3. CI 检查必须通过  
4. 禁止直接向受保护分支（main、develop）提交  
5. 合并前分支必须保持最新  
6. 合并后删除分支  

## 分支保护规则

### main & develop
- 要求 Pull Request 审查  
- 要求状态检查通过  
- 要求分支保持最新  
- 管理员同样受限制  
- 禁止强制推送  
- 禁止删除分支  

## 发布流程

1. 从 develop 创建 release 分支  
2. 提升版本号  
3. 修复所有发布特定问题  
4. 向 main 创建 PR  
5. 合并到 main 后：  
   - 打版本标签  
   - 反向合并回 develop  
   - 删除 release 分支  

## 热修复流程

1. 从 main 创建 hotfix 分支  
2. 修复问题  
3. 提升补丁版本号  
4. 向 main 创建 PR  
5. 合并到 main 后：  
   - 打版本标签  
   - 反向合并回 develop  
   - 删除 hotfix 分支